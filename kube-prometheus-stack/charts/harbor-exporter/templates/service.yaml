{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "harbor-exporter.fullname" . }}
  labels:
    app: harbor-exporter
spec:
  type: {{ .Values.service.type }}
  selector:
    app: harbor-exporter
  ports:
  - name: metrics
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
{{- end }}
